#include "../include/scheduler.h"
void run_round_robin(Process proc[], int n, int quantum) {
    // Sinh viên sẽ điền code vào đây. 
    // Để test thử hệ thống hoạt động, bạn điền tạm:
    proc[0].waiting_time = 6; proc[1].waiting_time = 4; proc[2].waiting_time = 7;
}
